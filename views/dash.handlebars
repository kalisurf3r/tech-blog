{{#each Blogs}}

<div class="row">
    <div class="col s12">
      <div class="card">
        <div class="card-content black-text">
          <span class="card-title">{{Blogs.date}}</span>
          <p>{{Blogs.content}}</p>
        </div>
      </div>
    </div>
  </div>
    {{/each}}

 <div class="center">
<a href="/addpost" class="btn-large waves-effect waves-light red">Add a Post</a>
</div>   