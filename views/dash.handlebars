{{#each Blogs}}
<div class="row">
    <div class="col s12">
      <div class="card">
        <div class="card-content black-text">
          <span class="card-title">{{this.createdAt}}</span>
          <p>{{this.content}}</p>
        </div>
      </div>
    </div>
  </div>
    {{/each}}

 <div class="center">
<a href="/addpost" class="btn-large waves-effect waves-light red">Add a Post</a>
</div>   
<script src="/js/addpost.js"></script>