{{#each Blogs}}

<div class="row">
    <div class="col s12">
      <div class="card">
        <div class="card-content black-text">
          <span class="card-title">{{this.title}} Written By: {{this.User.username}}</span>
          <p>{{this.content}}</p>
        </div>
        <div class="card-action">
          <a href="#">See full post</a>
        </div>
      </div>
    </div>
  </div>
    {{/each}}

    