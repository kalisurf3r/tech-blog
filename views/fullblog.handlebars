
<div class="row">
    <div class="col s12">
      <div class="card">
        <div class="card-content black-text">
          <span class="card-title">{{title}} Written By: {{User.username}} On: {{createdAt}}</span>
          <p>{{content}}</p>
        </div>
      </div>
    </div>
  </div>
  

    